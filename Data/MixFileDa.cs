using Entities;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Data
{
    public class MixFileDa
    {
        public bool upload(MixFile mixFile)
        {
            return false;
        }

        public bool download(MixFile mixFile)
        {
            return false;
        }

        public DataTable search(MixFile mixFile)
        {
            return null;
        }

        public bool modified(MixFile mixFile)
        {
            return false;
        }

        public bool delete(MixFile mixFile)
        {
            return false;
        }
    }
}
